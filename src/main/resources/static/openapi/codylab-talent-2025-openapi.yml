openapi: 3.1.0
info:
  title: CodyLab Talent 2025 API
  description: API per la gestione del progetto CodyLab Talent 2025.
  version: 1.0.0
servers:
  - url: http://localhost:8090/api/v1
    description: Server locale di sviluppo
tags:
  - name: Projects
    description: Gestione dei progetti
  - name: Tasks
    description: Gestione delle attività
  - name: Users
    description: Gestione degli utenti
  - name: Slots
    description: Gestione degli slot di lavoro
paths:
  /projects:
    get:
      summary: Recupera tutti i progetti
      description: Restituisce un elenco di progetti.
      operationId: searchProjects
      tags:
        - Projects
      parameters:
        - $ref: "#/components/parameters/PageNumber"
        - $ref: "#/components/parameters/Size"
        - $ref: "#/components/parameters/Sort"
        - name: projectFilter
          in: query
          description: Filtro per i progetti basato su ID utente o ID progetto.
          required: false
          style: form
          schema:
            $ref: '#/components/schemas/ProjectFilter'
      responses:
        '200':
          description: Elenco dei progetti recuperato con successo.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Projects'
        '204':
          description: No content - nessun progetto trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    post:
      summary: Crea un nuovo progetto
      description: Crea un nuovo progetto con i dettagli forniti.
      operationId: createProject
      tags:
        - Projects
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Projects'
      responses:
        '201':
          description: Progetto creato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Projects'
        '400':
          description: Richiesta non valida - dati del progetto mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /projects/{projectId}:
    get:
      summary: Recupera un progetto specifico
      description: Restituisce i dettagli di un progetto specifico.
      operationId: getProjectById
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          description: ID del progetto da recuperare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Progetto recuperato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Projects'
        '404':
          description: Progetto non trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
    put:
      summary: Aggiorna un progetto esistente
      description: Aggiorna i dettagli di un progetto esistente.
      operationId: updateProject
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          description: ID del progetto da aggiornare.
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Projects'
      responses:
        '200':
          description: Progetto aggiornato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Projects'
        '400':
          description: Richiesta non valida - dati del progetto mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          description: Progetto non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
    delete:
      summary: Elimina un progetto
      description: Elimina un progetto specifico.
      operationId: deleteProject
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          description: ID del progetto da eliminare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Progetto eliminato con successo.
        '404':
          description: Progetto non trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

  /tasks:
    get:
      summary: Recupera tutti i task
      description: Restituisce un elenco di task.
      operationId: searchTasks
      tags:
        - Tasks
      parameters:
        - $ref: "#/components/parameters/PageNumber"
        - $ref: "#/components/parameters/Size"
        - $ref: "#/components/parameters/Sort"
        - name: projectId
          in: query
          description: Filtro per ID progetto
          required: false
          schema:
            type: long


        - name: taskId
          in: query
          description: Filtro per ID task
          required: false
          schema:
            type: long

      responses:
        '200':
          description: Elenco dei task recuperato con successo.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Tasks'
        '204':
          description: No content - nessun task trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    post:
      summary: Crea un nuovo task
      description: Crea un nuovo task con i dettagli forniti.
      operationId: createTask
      tags:
        - Tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Tasks'
      responses:
        '201':
          description: Task creato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tasks'
        '400':
          description: Richiesta non valida - dati del task mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

  /tasks/{taskId}:
    get:
      summary: Recupera un task specifico
      description: Restituisce i dettagli di un task specifico.
      operationId: getTaskById
      tags:
        - Tasks
      parameters:
        - name: taskId
          in: path
          required: true
          description: ID del task da recuperare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Task recuperato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tasks'
        '404':
          description: Task non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    put:
      summary: Aggiorna un task esistente
      description: Aggiorna i dettagli di un task esistente.
      operationId: updateTask
      tags:
        - Tasks
      parameters:
        - name: taskId
          in: path
          required: true
          description: ID del task da aggiornare.
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Tasks'
      responses:
        '200':
          description: Task aggiornato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tasks'
        '400':
          description: Richiesta non valida - dati del task mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          description: Task non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    delete:
      summary: Elimina un task
      description: Elimina un task specifico.
      operationId: deleteTask
      tags:
        - Tasks
      parameters:
        - name: taskId
          in: path
          required: true
          description: ID del task da eliminare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Task eliminato con successo.
        '404':
          description: Task non trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

  /users:
    get:
      summary: Recupera tutti gli utenti
      description: Restituisce un elenco di utenti.
      operationId: searchUsers
      tags:
        - Users
      parameters:
        - $ref: "#/components/parameters/PageNumber"
        - $ref: "#/components/parameters/Size"
        - $ref: "#/components/parameters/Sort"
        - name: userFilter
          in: query
          description: Filtro per gli utenti basato su ID o profilo.
          required: false
          style: form
          schema:
            $ref: '#/components/schemas/UserFilter'
      responses:
        '200':
          description: Elenco degli utenti recuperato con successo.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Users'
        '204':
          description: No content - nessun utente trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    post:
      summary: Crea un nuovo utente
      description: Crea un nuovo utente con i dettagli forniti.
      operationId: createUser
      tags:
        - Users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Users'
      responses:
        '201':
          description: Utente creato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Users'
        '400':
          description: Richiesta non valida - dati dell'utente mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

  /users/{userId}:
    get:
      summary: Recupera un utente specifico
      description: Restituisce i dettagli di un utente specifico.
      operationId: getUserById
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          description: ID dell'utente da recuperare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Utente recuperato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Users'
        '404':
          description: Utente non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    put:
      summary: Aggiorna un utente esistente
      description: Aggiorna i dettagli di un utente esistente.
      operationId: updateUser
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          description: ID dell'utente da aggiornare.
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Users'
      responses:
        '200':
          description: Utente aggiornato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Users'
        '400':
          description: Richiesta non valida - dati dell'utente mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          description: Utente non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    delete:
      summary: Elimina un utente
      description: Elimina un utente specifico.
      operationId: deleteUser
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          description: ID dell'utente da eliminare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Utente eliminato con successo.
        '404':
          description: Utente non trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /slots:
    get:
      summary: Recupera tutti gli slot
      description: Restituisce un elenco di slot di lavoro.
      operationId: searchSlots
      tags:
        - Slots
      parameters:
        - $ref: "#/components/parameters/PageNumber"
        - $ref: "#/components/parameters/Size"
        - $ref: "#/components/parameters/Sort"
        - name: taskId
          in: query
          description: Filtro per ID task
          required: false
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Elenco degli slot recuperato con successo.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Slots'
        '204':
          description: No content - nessuno slot trovato.
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    post:
      summary: Crea un nuovo slot
      description: Crea un nuovo slot di lavoro con i dettagli forniti.
      operationId: createSlot
      tags:
        - Slots
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Slots'
      responses:
        '201':
          description: Slot creato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Slots'
        '400':
          description: Richiesta non valida - dati dello slot mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

  /slots/{slotId}:
    get:
      summary: Recupera uno slot specifico
      description: Restituisce i dettagli di uno slot specifico.
      operationId: getSlotById
      tags:
        - Slots
      parameters:
        - name: slotId
          in: path
          required: true
          description: ID dello slot da recuperare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Slot recuperato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Slots'
        '404':
          description: Slot non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    put:
      summary: Aggiorna uno slot esistente
      description: Aggiorna i dettagli di uno slot esistente.
      operationId: updateSlot
      tags:
        - Slots
      parameters:
        - name: slotId
          in: path
          required: true
          description: ID dello slot da aggiornare.
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Slots'
      responses:
        '200':
          description: Slot aggiornato con successo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Slots'
        '400':
          description: Richiesta non valida - dati dello slot mancanti o non validi.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          description: Slot non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'

    delete:
      summary: Elimina uno slot
      description: Elimina uno slot specifico.
      operationId: deleteSlot
      tags:
        - Slots
      parameters:
        - name: slotId
          in: path
          required: true
          description: ID dello slot da eliminare.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Slot eliminato con successo.
        '404':
          description: Slot non trovato.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          description: Errore interno del server.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'






components:
  headers:
    total-count:
      description: Total number of items in the response
      schema:
        type: integer
        format: int32
        examples:
          - value: 100
            summary: Total count of vehicles
    page-size:
      description: Number of items per page
      schema:
        type: integer
        format: int32
        examples:
          - value: 10
            summary: Default page size for vehicle listings
    page-number:
      description: Current page number (from 0)
      schema:
        type: integer
        format: int32
        examples:
          - value: 0
            summary: Current page number in the response
  parameters:
    PageNumber:
      name: pageNumber
      in: query
      description: Page number (starting from 0)
      required: false
      schema:
        type: integer
        format: int32
        default: 0
        examples:
          - value: 0
            summary: First page of results
    Size:
      name: size
      in: query
      description: Number of items per page
      required: false
      schema:
        type: integer
        format: int32
        default: 10
        examples:
          - value: 10
            summary: Default number of items per page
    Sort:
      name: sort
      in: query
      description: Sort criteria in the format 'property,asc|desc'
      required: false
      schema:
        type: string
        examples:
          - value: 'registrationNumber,asc'
            summary: Sort vehicles by registration number in ascending order
          - value: 'model,desc'
            summary: Sort vehicles by model in descending order

  schemas:
    Projects:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: ID del progetto.
        codice:
          type: string
          maxLength: 10
          description: Codice identificativo del progetto.
        nome:
          type: string
          description: Nome del progetto.
        descrizione:
          type: string
          description: Descrizione del progetto.
        dataInizio:
          type: string
          format: date
          description: Data di inizio del progetto.
          examples:
            - value: '2025-01-01'
              summary: Data di inizio del progetto
        durata:
          type: integer
          description: Durata del progetto in giorni.

        responsabileId:
          type: long
          description: ID dell'utente responsabile del progetto

        tasks:
          type: array
          items:
            $ref: '#/components/schemas/Tasks'
    Tasks:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: ID del task.
        progettoId:
          type: integer
          format: int64
          description: ID del progetto associato al task.
        codice:
          type: string
          maxLength: 10
          description: Codice identificativo del task.
        nome:
          type: string
          description: Nome del task.
        descrizione:
          type: string
          description: Descrizione del task.
        dataInizio:
          type: string
          format: date
          description: Data di inizio del task.

        durata:
          type: integer
          description: Durata del task in ore.



    Users:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: ID dell'utente.
        nome:
          type: string
          description: Nome dell'utente.
        cognome:
          type: string
          description: Cognome dell'utente.
        email:
          type: string
          format: email
          description: Indirizzo email dell'utente.
        profilo:
          type: string
          description: Ruolo dell'utente nel progetto.
        orarioGiornaliero:
          type: number
          format: double
          description: Ore giornaliere dedicate al progetto.

    Slots:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: ID dello slot.

        taskId:
          type: integer
          format: int64
          description: ID del task associato allo slot.

        startTime:
          type: string
          format: date-time
          description: Data e ora di inizio dello slot.
        endTime:
          type: string
          format: date-time
          description: Data e ora di fine dello slot.

    TaskFilter:
      type: object
      properties:
        projectId:
          type: integer
          format: int64
          description: ID del progetto associato al task.
        ids:
          type: array
          items:
            type: integer
            format: int64
          description: Elenco degli ID dei task da filtrare.
    ProjectFilter:
      type: object
      properties:
        userId:
          type: integer
          format: int64
          description: ID dell'utente associato al progetto.
        ids:
          type: array
          items:
            type: integer
            format: int64
          description: Elenco degli ID dei progetti da filtrare.

    UserFilter:
      type: object
      properties:
        ids:
          type: array
          items:
            type: integer
            format: int64
          description: Elenco degli ID degli utenti da filtrare.
        profilo:
          type: string
          description: Ruolo dell'utente nel progetto.

    SlotFilter:
      type: object
      properties:
        taskId:
          type: integer
          format: int64
          description: ID del task associato allo slot.
        ids:
          type: array
          items:
            type: integer
            format: int64
          description: Elenco degli ID degli slot da filtrare.

    Problem:
      description: error occurred - see status code and problem object for more information.
      type: object
      properties:
        type:
          type: string
          format: uri
          description: |
            An absolute URI that identifies the problem type.  When dereferenced,
            it SHOULD provide human-readable documentation for the problem type
            (e.g., using HTML).
          default: "about:blank"
          example: "https://zalando.github.io/problem/constraint-violation"
        title:
          type: string
          description: |
            A short, summary of the problem type. Written in english and readable
            for engineers (usually not suited for non technical stakeholders and
            not localized); example: Service Unavailable
        status:
          type: integer
          format: int32
          description: |
            The HTTP status code generated by the origin server for this occurrence
            of the problem.
          minimum: 100
          maximum: 600
          exclusiveMaximum: true
          example: 503
        detail:
          type: string
          description: |
            A human readable explanation specific to this occurrence of the
            problem.
          example: Connection to database timed out
        instance:
          type: string
          format: uri
          description: |
            An absolute URI that identifies the specific occurrence of the problem.
            It may or may not yield further information if dereferenced.
        errorCategory:
          type: string
          description: The category of the error.
          example: Invalid
        fieldName:
          type: string
          description: The name of the field that caused the error.
          example: phone
        timestamp:
          type: string
          format: date-time
          description: The timestamp when the error occurred.
          example: "2024-11-13T10:42:21.263191453Z"
        traceId:
          type: string
          description: The trace ID for the error.
          example: "057b351ca8e8badf158357ecbb2edb73"

