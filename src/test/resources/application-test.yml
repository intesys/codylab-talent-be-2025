codylab:
  datasource:
    url: jdbc:h2:mem:codylab-2025;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS WEB
    username: sa
    password: password
    driver: org.h2.Driver

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true